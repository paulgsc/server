[package]
name = "some-services"
description = "Shared service definitions"
categories = ["development-tools"]
keywords = ["services", "shared"]
readme = "README.md"
version.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
license.workspace = true
edition.workspace = true

[dependencies]
axum.workspace = true
thiserror.workspace = true
dashmap = "6.1.0"

# The rate limiter is sync, but its tests need a runtime to sleep through a
# refill window, so tokio is a test-only dependency here.
[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt", "time"] }

[lints]
workspace = true
