modules:
  tcp_connect:
    prober: tcp
    timeout: 10s

  http_2xx:
    prober: http
    timeout: 10s
    http:
      valid_http_versions: ["HTTP/1.1", "HTTP/2"]
      valid_status_codes: [101, 200, 429, 503]  # Treat WebSocket upgrade as "up"
      method: GET
